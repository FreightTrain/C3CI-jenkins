---
meta:
  environment: NAME
  dns_root: microbosh
  network_name: jenkins1
  security_groups:
    - jenkins

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: jenkins
    version: latest
